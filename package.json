{
  "name": "@shootismoke/backend",
  "author": "Amaury Martiny <amaury.martiny@protonmail.com>",
  "description": "Backend for Sh**t! I Smoke",
  "license": "GPL-3.0",
  "private": true,
  "repository": "https://github.com/shootismoke/backend",
  "scripts": {
    "prebuild": "yarn clean",
    "build": "tsc --build packages/aqi packages/dataproviders packages/graphql",
    "clean": "rimraf packages/**/lib packages/**/tsconfig.tsbuildinfo",
    "predeploy": "yarn build",
    "deploy": "lerna version --conventional-commits && lerna publish from-git",
    "lint": "tsc --noEmit && eslint --ext ts .",
    "postinstall": "yarn build",
    "start": "now dev",
    "test": "jest"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@amaurymartiny/config": "^1.0.7",
    "@types/jest": "^24.0.21",
    "jest": "^24.9.0",
    "lerna": "^3.18.3",
    "now": "^16.5.2",
    "ts-jest": "^24.1.0",
    "typescript": "^3.7.2"
  }
}
